{
  "name": "puku-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "Puku Canvas — an AI-aware infinite canvas for diagramming",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "vercel-build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
